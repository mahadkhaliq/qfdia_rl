# Q-NPG-FDIA dependencies (CPU-only; no torch required)
pennylane>=0.40
pennylane-lightning>=0.40   # optional fast simulator (lightning.qubit)
pandapower>=2.14
scipy>=1.10
numpy>=1.24
pandas>=2.0
pyarrow>=14.0              # parquet (QGrid-Synth dataset export)
# numba is optional but gives pandapower a large speedup:
# numba>=0.59
